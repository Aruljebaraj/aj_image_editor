
import 'package:flutter/material.dart';
class TextOverlay {
  double left;
  double top;
  String text;
  Color color;

  TextOverlay(
      {required this.left,
        required this.top,
        required this.text,
        required this.color});
}